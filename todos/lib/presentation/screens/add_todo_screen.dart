import 'package:flutter/material.dart';

class AddTodoScrren extends StatelessWidget {
  const AddTodoScrren({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Add Todo Screen"),
      ),
    );
  }
}